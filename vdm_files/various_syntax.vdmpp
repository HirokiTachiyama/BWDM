
class problem_example

types

stack= token;
prime_numbers = seq of int;
initeger_numbers ::
        positive_numbers : nat1
        ZERO : int
        negative_numbers : int;

functions

problem_function : nat -> seq of char
    problem_function(a) ==
        if(a mod 4 = 0) then
            if(a > 92) then
      	    	 "96, 100, 104, ..."
	    else
	          "..., 84, 88, 92"
	else
	    "others"

	pre a > 5
	post 10 > a;

end problem_example
